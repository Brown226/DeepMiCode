# DeepMiCode 更新日志

本文件记录 DeepMiCode 项目的版本功能更新、改进和 Bug 修复。
格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

---

## [未发布]

### 新增
- 专属中文更新日志文件 `更新日志.md`。
- 桌面端改造分析文档 `desktop/桌面端改造分析文档.md`（含 cc-haha 0.3.2 深度对比）。
- **桌面端：Zustand 状态管理** — 新增 `uiStore`（主题/字体/货币/侧边栏/modal/toast）、`tabStore`（标签页管理）、`updateStore`（自动更新）、`startupStore`（启动失败状态）。
- **桌面端：新增快捷键** — `Cmd+/` 切换侧边栏、`Cmd+.` 切换上下文面板、`Cmd+1-9` 切换标签页、`Cmd+Shift+C` 复制最后一条助手回复。
- **桌面端：Context Panel 折叠** — 上下文面板支持折叠/展开，默认显示 token 用量条，用户选择记忆到 localStorage。
- **桌面端：设置面板搜索** — 顶部搜索框支持按页面名称和描述过滤，自动跳转到匹配页面。
- **桌面端：工作区文件预览** — 上下文面板文件列表新增预览按钮，可查看文件内容（支持代码高亮）。
- **桌面端：Mermaid 图表渲染** — 助手输出中的 Mermaid 代码块自动渲染为可视化图表。

### 修复
- _暂无_

### 变更
- **桌面端：App.tsx 瘦身** — 6 个 useState 迁移到 Zustand store，4 个 localStorage 同步 useEffect 合并为 1 个初始化 effect。
- 更新改造文档，记录实施进度和待办事项。
- 更新 `DEEPMICODE.md`：修正 React 18→19、TS 5.6+→5.9+、prepublishOnly 执行顺序、补全缺失目录、新增 workspace 包说明、补充 npm 脚本和 git hooks、Dashboard 标注为独立子项目。
- **桌面端：App.tsx 瘦身** — 6 个 useState 迁移到 Zustand store，4 个 localStorage 同步 useEffect 合并为 1 个初始化 effect。

---

> 从下一个版本开始，所有功能更新和 Bug 修复将同步记录在此文件中。
